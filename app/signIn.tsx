import React, { FC } from 'react';
import { Text, View } from 'react-native';

const SignIn: FC = () => {
    return (
        <View>
            <Text>signIn</Text>
        </View>
    );
};

export default SignIn;
