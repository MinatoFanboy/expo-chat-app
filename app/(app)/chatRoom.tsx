import React, { FC } from 'react';
import { Text, View } from 'react-native';

const ChatRoom: FC = () => {
    return (
        <View>
            <Text>ChatRoom</Text>
        </View>
    );
};

export default ChatRoom;
