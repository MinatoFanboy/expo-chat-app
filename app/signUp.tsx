import React, { FC } from 'react';
import { Text, View } from 'react-native';

const SignUp: FC = () => {
    return (
        <View>
            <Text>signUp</Text>
        </View>
    );
};

export default SignUp;
